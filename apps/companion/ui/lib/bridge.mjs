const registered = Object.create(null);
export function registerFunctions(functions) { Object.assign(registered, functions); }
export const showStart = (...args) => { if (!registered.showStart) throw Error('Renderer not registered: showStart'); return registered.showStart(...args); };
export const showProjects = (...args) => { if (!registered.showProjects) throw Error('Renderer not registered: showProjects'); return registered.showProjects(...args); };
export const duplicate = (...args) => { if (!registered.duplicate) throw Error('Renderer not registered: duplicate'); return registered.duplicate(...args); };
export const showHelp = (...args) => { if (!registered.showHelp) throw Error('Renderer not registered: showHelp'); return registered.showHelp(...args); };
export const showTerm = (...args) => { if (!registered.showTerm) throw Error('Renderer not registered: showTerm'); return registered.showTerm(...args); };
export const showPrivacy = (...args) => { if (!registered.showPrivacy) throw Error('Renderer not registered: showPrivacy'); return registered.showPrivacy(...args); };
export const startSetup = (...args) => { if (!registered.startSetup) throw Error('Renderer not registered: startSetup'); return registered.startSetup(...args); };
export const showSetup = (...args) => { if (!registered.showSetup) throw Error('Renderer not registered: showSetup'); return registered.showSetup(...args); };
export const showFolder = (...args) => { if (!registered.showFolder) throw Error('Renderer not registered: showFolder'); return registered.showFolder(...args); };
export const INSPIRATION_CHIPS = (...args) => { if (!registered.INSPIRATION_CHIPS) throw Error('Renderer not registered: INSPIRATION_CHIPS'); return registered.INSPIRATION_CHIPS(...args); };
export const masterActivationPrompt = (...args) => { if (!registered.masterActivationPrompt) throw Error('Renderer not registered: masterActivationPrompt'); return registered.masterActivationPrompt(...args); };
export const showDelimitation = (...args) => { if (!registered.showDelimitation) throw Error('Renderer not registered: showDelimitation'); return registered.showDelimitation(...args); };
export const showVision = (...args) => { if (!registered.showVision) throw Error('Renderer not registered: showVision'); return registered.showVision(...args); };
export const showInstall = (...args) => { if (!registered.showInstall) throw Error('Renderer not registered: showInstall'); return registered.showInstall(...args); };
export const visionObjective = (...args) => { if (!registered.visionObjective) throw Error('Renderer not registered: visionObjective'); return registered.visionObjective(...args); };
export const executeInstallation = (...args) => { if (!registered.executeInstallation) throw Error('Renderer not registered: executeInstallation'); return registered.executeInstallation(...args); };
export const showFinished = (...args) => { if (!registered.showFinished) throw Error('Renderer not registered: showFinished'); return registered.showFinished(...args); };
export const changes = (...args) => { if (!registered.changes) throw Error('Renderer not registered: changes'); return registered.changes(...args); };
export const showBaseReview = (...args) => { if (!registered.showBaseReview) throw Error('Renderer not registered: showBaseReview'); return registered.showBaseReview(...args); };
export const reviewStack = (...args) => { if (!registered.reviewStack) throw Error('Renderer not registered: reviewStack'); return registered.reviewStack(...args); };
export const showStackReview = (...args) => { if (!registered.showStackReview) throw Error('Renderer not registered: showStackReview'); return registered.showStackReview(...args); };
export const notOfferedPanel = (...args) => { if (!registered.notOfferedPanel) throw Error('Renderer not registered: notOfferedPanel'); return registered.notOfferedPanel(...args); };
export const stackPanel = (...args) => { if (!registered.stackPanel) throw Error('Renderer not registered: stackPanel'); return registered.stackPanel(...args); };
export const stackName = (...args) => { if (!registered.stackName) throw Error('Renderer not registered: stackName'); return registered.stackName(...args); };
export const reviewEngineering = (...args) => { if (!registered.reviewEngineering) throw Error('Renderer not registered: reviewEngineering'); return registered.reviewEngineering(...args); };
export const downloadSize = (...args) => { if (!registered.downloadSize) throw Error('Renderer not registered: downloadSize'); return registered.downloadSize(...args); };
export const reviewRepair = (...args) => { if (!registered.reviewRepair) throw Error('Renderer not registered: reviewRepair'); return registered.reviewRepair(...args); };
export const showEnvironmentReview = (...args) => { if (!registered.showEnvironmentReview) throw Error('Renderer not registered: showEnvironmentReview'); return registered.showEnvironmentReview(...args); };
export const showEngineeringReview = (...args) => { if (!registered.showEngineeringReview) throw Error('Renderer not registered: showEngineeringReview'); return registered.showEngineeringReview(...args); };
export const showActivationReview = (...args) => { if (!registered.showActivationReview) throw Error('Renderer not registered: showActivationReview'); return registered.showActivationReview(...args); };
export const prepareContext = (...args) => { if (!registered.prepareContext) throw Error('Renderer not registered: prepareContext'); return registered.prepareContext(...args); };
export const showContextReview = (...args) => { if (!registered.showContextReview) throw Error('Renderer not registered: showContextReview'); return registered.showContextReview(...args); };
export const showSyncReview = (...args) => { if (!registered.showSyncReview) throw Error('Renderer not registered: showSyncReview'); return registered.showSyncReview(...args); };
export const showFinalContext = (...args) => { if (!registered.showFinalContext) throw Error('Renderer not registered: showFinalContext'); return registered.showFinalContext(...args); };
export const reviewCode = (...args) => { if (!registered.reviewCode) throw Error('Renderer not registered: reviewCode'); return registered.reviewCode(...args); };
export const showCodeReview = (...args) => { if (!registered.showCodeReview) throw Error('Renderer not registered: showCodeReview'); return registered.showCodeReview(...args); };
export const resaveBase = (...args) => { if (!registered.resaveBase) throw Error('Renderer not registered: resaveBase'); return registered.resaveBase(...args); };
export const openProject = (...args) => { if (!registered.openProject) throw Error('Renderer not registered: openProject'); return registered.openProject(...args); };
export const forget = (...args) => { if (!registered.forget) throw Error('Renderer not registered: forget'); return registered.forget(...args); };
export const statusCard = (...args) => { if (!registered.statusCard) throw Error('Renderer not registered: statusCard'); return registered.statusCard(...args); };
export const guidePanel = (...args) => { if (!registered.guidePanel) throw Error('Renderer not registered: guidePanel'); return registered.guidePanel(...args); };
export const showWorkspace = (...args) => { if (!registered.showWorkspace) throw Error('Renderer not registered: showWorkspace'); return registered.showWorkspace(...args); };
export const recovery = (...args) => { if (!registered.recovery) throw Error('Renderer not registered: recovery'); return registered.recovery(...args); };
export const searchView = (...args) => { if (!registered.searchView) throw Error('Renderer not registered: searchView'); return registered.searchView(...args); };
export const previewExport = (...args) => { if (!registered.previewExport) throw Error('Renderer not registered: previewExport'); return registered.previewExport(...args); };
export const codeSearchView = (...args) => { if (!registered.codeSearchView) throw Error('Renderer not registered: codeSearchView'); return registered.codeSearchView(...args); };
export const modelPanel = (...args) => { if (!registered.modelPanel) throw Error('Renderer not registered: modelPanel'); return registered.modelPanel(...args); };
export const investigationPanel = (...args) => { if (!registered.investigationPanel) throw Error('Renderer not registered: investigationPanel'); return registered.investigationPanel(...args); };
export const handoffView = (...args) => { if (!registered.handoffView) throw Error('Renderer not registered: handoffView'); return registered.handoffView(...args); };
