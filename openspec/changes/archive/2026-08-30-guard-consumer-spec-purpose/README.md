# guard-consumer-spec-purpose

Publish the spec Purpose gate through `src/` and wire it into `opsx-check`, so a bootstrapped repository
observes the same failure the upstream already observes when an archived capability keeps the text that
OpenSpec seeds under `## Purpose`.
