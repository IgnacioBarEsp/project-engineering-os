Companion must activate and verify the tools its preparation promises. Preserve product dependencies,
use reviewed local runtimes and optional code context, retain document retrieval, and provide a working
handoff to the existing AI with honest readiness and recoverable failures.
