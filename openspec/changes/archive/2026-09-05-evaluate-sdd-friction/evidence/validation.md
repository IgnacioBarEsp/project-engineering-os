# Validation — 2026-09-05

PASS: enriched issues #14 and #15 each pass 13 DoR checks; exact local OpenSpec 1.6.0 validates all nine
current items strictly. Full repository check passes package, neutrality, documentation, workflows, debt
and all 248 tests using npm 11.19.1 / Node 24.18.0 on Windows. No runtime tests were added for this
documentation change. The installed fixture is unchanged and remains exercised by protected PR CI.

Manual source inspection: PR #44 is merged and includes the Purpose placeholder gate; SELF_APPLICATION
records recurring findings without metrics. PR #67's archived evidence explicitly corrects the initial
spinner hypothesis to the macOS path identity cause. PR #69 passed the full matrix and is merged. Prompt
01 points 9 and 10 contain the inputs the guide reuses. The guide's illustrative rows are labeled examples.

All acceptance criteria are documented: optionality, product non-applicability with reason, prohibited AI,
owner/review/fallback, no inferred productivity, no telemetry, consumer-owned observations, deferred schema
and automatic adoption. The maintainer explicitly selected local evaluation and guidance. The external
benchmark is still pending; no completion of it is claimed.

Rollback is a normal documentation PR revert and does not affect consumer files, generated workflows,
private documents, runtime configuration or remote trackers. Required CI remains enforced at integration.
