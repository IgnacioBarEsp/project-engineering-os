Issue #89 makes an explicitly selected local engineering toolchain usable without changing product
dependencies. Wrapper, OPSX and doctor must agree on its pinned identity; the default root layout stays.
