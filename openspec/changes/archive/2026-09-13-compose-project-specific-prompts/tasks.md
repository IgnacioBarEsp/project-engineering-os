## 1. Baseline and readiness

- [x] 1.1 Run the propose-phase readiness gate for issue 99 and record the result. **13 PASS, 0 FAIL.**
- [x] 1.2 Write the brownfield baseline from the code on `main` at bfedb7f: the single template, what issue 98
      left as this change's input, what the inventory already knows, and what talks to the network today.
- [x] 1.3 Resolve the issue's open question about level 2 shipping on or off, with the reason, in the design.
      **Off: free tiers commonly train on what they receive, and nobody can accept that for someone else.**
- [x] 1.4 Record what may leave this machine, as a shape rather than as a filter.

## 2. The prompt engine

- [x] 2.1 Add `context/prompts.mjs`: a pure `composePrompt` over profile, experience, role, goal, chosen AI,
      pending stages and the aggregated inventory.
- [x] 2.2 Add the inventory aggregate — extension, kind and count plus totals — with no path in it.
- [x] 2.3 Replace the single template that every handover used. **The per-recipe texts of issue 98 are a
      different thing and stay; the proposal says so after a review caught it claiming both.**
- [x] 2.4 Tests: the five profiles produce different prompts, compared by content; the same inputs produce the
      same text; no path reaches the aggregate; the prompt names what is pending and what not to assume.

## 3. The client and the four levels

- [x] 3.1 Add `runtime/inference.mjs`: one OpenAI-compatible client in the main process, with an explicit
      destination allowlist, a timeout, a maximum response size, no redirects and no credentials in a URL.
- [x] 3.2 Detect a local model by asking the loopback endpoint for its model list with a short timeout.
      **12 ms, 14 models on this machine.**
- [x] 3.3 Add the payload guard that refuses to send anything containing a file path or file content.
      **Comparing normalised and lowercased, after a review walked a name past it with a different case.**
- [x] 3.4 Make the template the floor: validate what comes back, compare it to the draft, and discard it when
      it is invalid, empty, shorter or off-topic. **And append this application's rules after whatever a model
      returns, because that text goes to an AI that can open the folder.**
- [x] 3.5 Degrade to the level below on a dead, slow or nonsensical provider, with the cause the client gave.
- [x] 3.6 Ship level 2 off, and put what each level sends on the screen that offers the prompt.
- [x] 3.7 Add the "ask your own AI to investigate the folder" prompt and accept its summary as an input.
      **That summary never leaves this machine, not even to a model.**

## 4. Evidence

- [x] 4.1 Keep the level-0 prompt for the five profiles as evidence. **1 994 – 2 768 characters against ~450
      before, generated by the same path the application delivers.**
- [x] 4.2 Intercept the request and fail on a path or on file content; add the mutation that removes the guard.
      **Six mutations now cover the client and the composition.**
- [x] 4.3 Measure level 1 against the local model actually running on this machine. **`qwen/qwen3-coder-30b`,
      13 000 ms, 732 characters — it did not clear the floor, so the template was used.**
- [x] 4.4 Measure a dead provider, a slow one and one that answers nonsense; report elapsed times. **2 ms,
      25 012 ms against a 25 000 ms bound, and 120 003 ms against the local 120 000 ms bound.**
- [x] 4.5 Run the five native journeys and the browser journeys with the rules from #97 and #98 still passing.
      **5 profiles 0 findings; 36 screens 0 findings; 39 interface mutations, 39 detected.**
- [x] 4.6 Independent adversarial review by a subagent that did not implement this change. **FAIL: 2 blockers
      and 9 majors. All resolved; the full verdict is in `evidence/independent-review.md`.**
- [x] 4.7 Capture the findings into the debt registry with the category each one actually has. **The plan
      paused at 5/5 and the pause was right: it made me re-read my own classification, which did not hold
      against this registry's own precedent. Back to 4/5 with the reasoning recorded.**

## 5. Closeout

- [x] 5.1 `openspec validate --all --strict` green.
- [x] 5.2 `readiness-check --phase archive` with 0 FAIL.
- [x] 5.3 Archive with the official CLI and write the seeded `## Purpose`.
- [x] 5.4 DCO-signed commits and a pull request against the protected branch, integrated by squash **only
      after `CI / required` is green**; nothing here touches that gate, branch protection or the installer.
- [x] 5.5 Close the issue saying what was demonstrated and what was not.
