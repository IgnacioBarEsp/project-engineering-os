# Baseline

Package 0.2.0 with the completed controls, install hardening, scoped rules and AI opportunity guide in main.
Tracker PR #71 is fully locally validated and awaiting required CI; it must integrate before this release.
Existing main protection enforces strict CI / required for admins, with zero required review approvals;
contributor text incorrectly states independent human approval is always mandatory. The maintainer has
explicitly delegated this bounded closeout. Private DOCX/PDF files remain untracked and excluded from pack.
