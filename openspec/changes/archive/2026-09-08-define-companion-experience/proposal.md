## Why

Issue #76 passes 13 DoR checks and starts the now-active program #66. The current terminal-first
constructor serves software repositories; researchers, creators and general users need a reusable visual
companion that prepares the right context and proves what is ready. The maintainer explicitly requested
implementation, derived issues, local installer QA and measured before/after comparisons on 2026-09-07.

## What Changes

- Define five project journeys, readiness levels and user-centered acceptance criteria.
- Compare desktop packaging and context engines; choose a bounded architecture with honest limitations.
- Produce a reversible interactive visual prototype, design preflight and accessible states.
- Define installer QA, adversarial review and benchmark methodology; organize implementation issues.

## Capabilities

### New Capabilities
- companion-experience: verified preparation contracts for a reusable desktop companion.

## Impact

Discovery documents, prototype and upstream UI evidence profile activation in this change. The neutral constructor and published 0.3.0 remain
unchanged. Implementations are owned by #77–#81. The maintainer confirmed external AI applications with
context export for web chat; a built-in chat is out of scope. Unknown conference date is not a deadline.
Optional external installations default to detect/configure/review while the interview answer is pending.
Approval follows the maintainer's explicit delegation; prototype review is not a representative user study.
Rollback reverts these discovery artifacts, retaining user data and the issue history.
