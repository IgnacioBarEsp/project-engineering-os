# OpenSpec strict — pre-apply

Ejecutado el 20 de septiembre de 2026 antes de iniciar `apply`:

```text
npx openspec validate fix-marker-homograph-detection --strict --no-interactive
Change 'fix-marker-homograph-detection' is valid
```

`npx openspec status --change fix-marker-homograph-detection` informó 4/4 artefactos completos: proposal,
design, specs y tasks.
