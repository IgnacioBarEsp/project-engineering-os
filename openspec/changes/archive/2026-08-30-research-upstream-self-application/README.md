# research-upstream-self-application

Publish a decision record that gives every mechanism the upstream distributes a verdict about applying it to
the upstream itself, resolves the ownership recursion with measurements instead of warnings, and breaks the
result into implementable issues. No adoption is executed here.
